// Host loader entry for the browser-only codex-side-outline plugin.
// Provides no host-side behavior; the outline reads the live conversation
// through the client `sessions` service and renders in `shell.overlay`.
export function apply() {}
